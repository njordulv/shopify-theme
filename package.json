{
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "vite": "^6.3.6",
    "vite-plugin-shopify": "^4.0.2"
  },
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "shopify:dev": "shopify theme dev --theme=$SHOPIFY_THEME_ID",
    "shopify:push": "shopify theme push --theme=$SHOPIFY_THEME_ID",
    "deploy": "npm run build && npm run shopify:push"
  }
}
